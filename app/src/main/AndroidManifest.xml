<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekpemmob">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjekPemmob">
        <activity android:name=".activity.activity_cart"></activity>
        <activity android:name=".activity.activity_food" />
        <activity android:name=".activity.activity_profile" />
        <activity android:name=".activity.activity_FoodList" />
        <activity android:name=".activity.activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.activity_register" />
        <activity android:name=".activity.activity_mainmenu" />
    </application>

</manifest>